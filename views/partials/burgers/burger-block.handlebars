<div class="row">
		<div class="col-6">
			{{burger_name}}
		</div>
		{{#unless devoured}}
		<div class="col-6">
			<button class="devour-burger" data-id="{{this.id}}">Devour!</button>
		</div>
		{{/unless}}
</div>